# On headless Linux, opencv-python pulls libGL which is not installed by default;
# opencv-python-headless ships the same cv2 module without the GUI deps.
opencv-python-headless
numpy
pillow
easyocr
simple-lama-inpainting

# Notes:
# - easyocr pulls in torch/torchvision (large). Install may take a while.
# - simple-lama-inpainting declares opencv-python as a dep; cv2 from
#   opencv-python-headless satisfies the import. Ignore the resolver warning.
# - After installing, verify with:
#     python3 -c "import cv2, numpy, PIL, easyocr; from simple_lama_inpainting import SimpleLama; print('ok')"
